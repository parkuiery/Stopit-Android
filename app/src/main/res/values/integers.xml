<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        소개 히어로 제목 크기.

        한글·일본어·중국어는 글자 하나가 라틴 문자보다 훨씬 많은 뜻을 담아서 같은 문장이
        절반 길이로 끝난다. 그래서 CJK 는 크게 키워도 한두 줄에 들어가지만, 라틴 문자로
        같은 크기를 쓰면 세 줄로 늘어나면서 히어로가 화면을 다 먹고 셀링 포인트가 접힘
        아래로 밀린다(스페인어 34sp 에서 실제로 그랬다).

        그래서 기본은 안전한 크기로 두고 CJK 로케일만 키운다.
    -->
    <integer name="pomodoro_intro_title_sp">28</integer>
</resources>
