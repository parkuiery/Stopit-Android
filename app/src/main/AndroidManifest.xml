<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!--
        Website blocking runs a local DNS VpnService (KeepDnsVpnService, declared below)
        while a lock is active. These three permissions belong to that feature.

        They lived in app/src/dev/AndroidManifest.xml between 1.8.1 and this release,
        because 1.8.0 shipped them to production while WEBSITE_BLOCKING_ENABLED was false
        and Play rejected the upload edit: a special-use foreground service must not sit in
        an AAB where users cannot reach it. The feature is enabled in prod now, so they
        belong here — and the Play Console App content -> Foreground service permissions
        declaration must stay filled in for as long as they do.

        FOREGROUND_SERVICE and ACCESS_NETWORK_STATE also arrive transitively through
        androidx.work and play-services-ads-api, so removing these lines would not fail the
        build. They are declared explicitly anyway: a dependency change must not be able to
        silently take the VPN's network callbacks away.

        See docs/WEBSITE_BLOCKING_VPN_SPIKE.md.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
        Stopit uses broad package visibility only for the app-selection picker where
        users choose launchable apps to block. The policy-sensitive query is isolated
        behind InstalledAppRepository and filtered by SelectableAppPolicy.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".KeepApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Keep"
        tools:targetApi="31">

        <!--
            targetSdk 36(Android 16)부터 sw>=600dp 화면에서는 activity의
            screenOrientation / resizableActivity / aspect ratio 제한이 무시된다.
            Stopit의 화면들은 아직 세로 기준으로만 설계·검증돼 있어, 태블릿·폴더블에서
            가로로 펼쳐졌을 때의 레이아웃 근거가 없다. 그래서 MainActivity의
            screenOrientation="portrait"를 API 36 동안 유지하는 한시적 opt-out을 건다.

            이 opt-out은 API 37에서 제거된다. 그전에 대형 화면 적응형 레이아웃을 실제로
            대응하고 이 property를 지워야 한다. 지우기 전 조건: 대형 화면 레이아웃 대응 +
            sw>=600dp 에뮬레이터/실기기 가로 검증.
        -->
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
            android:value="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Keep">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BlockActivity"
            android:exported="false" />

        <service
            android:name=".service.KeepAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
        <service
            android:name=".service.KeepMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!--
            Non-exported: the system binds this through BIND_VPN_SERVICE after the user
            approves the VpnService consent dialog. Nothing else may start it.
            The specialUse subtype string below is what Play reviews; keep it in sync with
            the Play Console foreground service declaration.
        -->
        <service
            android:name=".websiteblocking.KeepDnsVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="false" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Local DNS filtering blocks user-selected websites while a StopIt lock is active." />
        </service>

        <!--
            The focus session must keep running with the screen off and the app in the background:
            that is the whole point of a pomodoro cycle. The service also carries the ongoing
            notification that tells the user which phase they are in.

            Keep the specialUse subtype string in sync with the Play Console declaration.
        -->
        <service
            android:name=".service.PomodoroSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Runs the user-started focus/break cycle and shows its remaining time while StopIt keeps the selected apps blocked." />
        </service>

        <receiver
            android:name=".receiver.RoutineAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                tools:node="remove" />
        </provider>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${adMobApplicationId}"/>
    </application>

</manifest>
